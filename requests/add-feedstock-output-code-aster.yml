action: add_feedstock_output
feedstock_to_output_mapping:
  # this entry adds a package name
  - code-aster: code-aster
  # this entry adds another output to the same feedstock
  - code-aster: code-aster-tests
